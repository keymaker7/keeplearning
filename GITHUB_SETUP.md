# GitHub 업로드 가이드

## 📦 현재 준비 상태

✅ README.md 생성 완료
✅ .gitignore 업데이트 완료
✅ GitHub Actions CI 설정 완료
✅ 프로젝트 문서화 완료

## 🚀 GitHub에 업로드하는 방법

### 1. GitHub 저장소 생성

1. GitHub.com에 로그인
2. 우측 상단 "+" 버튼 클릭 → "New repository"
3. 저장소 이름 입력 (예: `HaksaengPyeongga`)
4. 설명 추가 (선택사항)
5. Public 또는 Private 선택
6. **"Initialize this repository with a README" 체크하지 않기** (이미 README.md가 있음)
7. "Create repository" 클릭

### 2. 로컬에서 Git 초기화 및 커밋

현재 프로젝트 디렉토리에서 다음 명령어 실행:

```bash
# Git 초기화 (이미 초기화되어 있으면 생략)
git init

# 현재 상태 확인
git status

# 모든 파일 추가 (제외된 파일은 .gitignore에 따라 자동 제외)
git add .

# 초기 커밋
git commit -m "Initial commit: 학생평가 시스템"

# GitHub 저장소를 원격 저장소로 추가
# YOUR_USERNAME과 YOUR_REPO_NAME을 실제 값으로 변경
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 또는 SSH를 사용하는 경우
# git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git

# 메인 브랜치 이름 확인 및 설정
git branch -M main

# GitHub에 푸시
git push -u origin main
```

### 3. 환경 변수 설정 가이드

GitHub에 업로드할 때는 `.env` 파일을 포함하지 않습니다 (`.gitignore`에 포함됨).

대신 `.env.example` 파일을 만들어서 필요한 환경 변수를 문서화하세요:

```bash
# .env.example 파일 생성
cat > .env.example << EOF
DATABASE_URL=postgresql://user:password@host:5432/database
OPENAI_API_KEY=sk-your-openai-api-key
PORT=5000
SESSION_SECRET=your-random-session-secret-key
EOF
```

이 파일을 커밋하면 다른 개발자들이 필요한 환경 변수를 알 수 있습니다.

### 4. 업로드 전 확인사항

#### ✅ .gitignore 확인
다음 항목들이 제외되는지 확인:
- `node_modules/`
- `.env` 파일들
- `dist/`
- `uploads/` 파일들
- `attached_assets/` 파일들

#### ✅ 민감한 정보 제거
코드 내에 하드코딩된 다음 정보가 있는지 확인:
- 데이터베이스 연결 문자열
- API 키
- 비밀번호
- 세션 시크릿

#### ✅ README.md 확인
- 프로젝트 설명이 명확한지
- 설치 방법이 정확한지
- 환경 변수 설정 방법이 있는지

### 5. 추가 권장사항

#### GitHub Actions 설정
`.github/workflows/ci.yml` 파일이 이미 생성되어 있습니다.
이 파일은 코드 푸시 시 자동으로 타입 체크와 빌드를 실행합니다.

#### Issues 및 Projects 설정
- Issues 탭에서 버그 리포트 및 기능 요청 관리
- Projects 탭에서 작업 관리

#### Releases 설정
- 주요 버전 업데이트 시 Releases 생성
- 태그를 사용하여 버전 관리

## 🔒 보안 체크리스트

업로드 전 다음을 확인하세요:

- [ ] `.env` 파일이 Git에 포함되지 않음
- [ ] 코드에 하드코딩된 API 키 없음
- [ ] 데이터베이스 연결 정보가 코드에 직접 포함되지 않음
- [ ] 비밀번호나 세션 시크릿이 코드에 포함되지 않음
- [ ] `.gitignore`가 올바르게 설정됨

## 📝 커밋 메시지 가이드

좋은 커밋 메시지 예시:

```
feat: 학생 일괄 생성 기능 추가
fix: 학습 기록 조회 시 권한 오류 수정
docs: README에 설치 방법 추가
refactor: API 라우트 구조 개선
style: 코드 포맷팅 수정
test: 학생 관리 API 테스트 추가
```

## 🐛 문제 해결

### "fatal: remote origin already exists" 오류
```bash
# 기존 원격 저장소 제거 후 재설정
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
```

### "permission denied" 오류
- GitHub 인증 확인 (Personal Access Token 또는 SSH 키)
- 저장소 권한 확인

### 대용량 파일 업로드 오류
- `uploads/` 및 `attached_assets/` 폴더는 `.gitignore`에 포함되어 있어야 함
- Git LFS 사용 고려 (대용량 파일의 경우)

## 📚 추가 리소스

- [GitHub Docs](https://docs.github.com/)
- [Git 기본 사용법](https://git-scm.com/book/ko/v2)
- [GitHub Actions](https://docs.github.com/en/actions)

## ✅ 업로드 완료 후

1. GitHub 저장소 페이지에서 README.md가 제대로 표시되는지 확인
2. Issues 및 Projects 설정
3. Collaborators 추가 (필요한 경우)
4. 배포 환경 설정 (필요한 경우)

